<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To do list</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
    />
  </head>
  <body>
    <main class="min-h-screen bg-stone-900">
      {{> nav}}

      <section
        class="max-w-[670px] m-auto rounded flex flex-col mt-12 p-5 gap-5"
      >
        <form
          action="/users/login"
          method="post"
          class="rounded-xl bg-[#232935] flex flex-col w-full max-w-[350px] m-auto p-5 text-white"
        >
          <h1 class="w-full text-center text-2xl mb-7 font-bold">Login</h1>
          {{#if errors}}
          <div class="bg-purple-600 text-black p-2 rounded-md mb-3">
            {{#each errors}}
            <p><i class="fa-solid fa-triangle-exclamation mr-2"></i>{{msg}}</p>
            {{/each}}
          </div>
          {{/if}}
          <label for="username" class="">Username</label>
          <input
            type="text"
            id="username"
            name="username"
            required
            class="w-full bg-stone-900 p-1 rounded-lg mb-3 mt-1 border-purple-800 border-2 border-solid"
          />
          <label for="password">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            required
            class="w-full bg-stone-900 p-1 rounded-lg mb-3 mt-1 border-purple-800 border-2 border-solid"
          />
          <button
            type="submit"
            class="w-1/2 bg-purple-800 p-2 m-auto rounded-lg hover:bg-purple-700 hover:scale-[.96] mt-3"
          >
            Login
          </button>
          <p class="mt-3 mx-auto">
            Don't have a account?
            <a href="/users/register" class="text-purple-600 underline hover:text-purple-400">Register</a>
            here
          </p>
        </form>
      </section>
    </main>
  </body>
</html>
